<UserControl x:Class="R54IN0.WPF.MeasureFieldEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="263*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="단위" Binding="{Binding Name}"/>
            </DataGrid.Columns>
        </DataGrid>
        <UniformGrid Grid.Row="1" Grid.Column="0" Columns="2" Rows="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Width="60" Content="추가" x:Name="ItemAddButton" Click="ItemAddButton_Click"/>
            <Button Width="60" Content="삭제" x:Name="ItemRemoveButton" Click="ItemRemoveButton_Click"/>
        </UniformGrid>
    </Grid>
</UserControl>
