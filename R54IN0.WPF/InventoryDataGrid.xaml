<UserControl x:Class="R54IN0.WPF.InventoryDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:R54IN0.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <!--- 코드, 품명, 규격, 총재고수량, 단위, 입고단가, 입고총금액, 출고단가, 출고총금액, 화폐, 비고-->
    <Grid>
        <DataGrid ItemsSource="{Binding InventoryItems}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="코드" Binding="{Binding Code}"/>
                <DataGridTextColumn Header="품명" Binding="{Binding ItemName}"/>
                <DataGridTextColumn Header="규격" Binding="{Binding Specification}"/>
                <DataGridTextColumn Header="수량" Binding="{Binding Count}"/>
                <DataGridTextColumn Header="단위" Binding="{Binding Measure}"/>
                <DataGridTextColumn Header="입고단가" Binding="{Binding SalesUnitPrice}"/>
                <DataGridTextColumn Header="입고총금액" Binding="{Binding SelesPriceAmount}"/>
                <DataGridTextColumn Header="출고단가" Binding="{Binding PurchaseUnitPrice}"/>
                <DataGridTextColumn Header="출고총금액" Binding="{Binding PurchasePriceAmount}"/>
                <DataGridTextColumn Header="화폐" Binding="{Binding Currency}"/>
                <DataGridTextColumn Header="보관장소" Binding="{Binding Warehousing}"/>
                <DataGridTextColumn Header="비고" Binding="{Binding Remark}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
